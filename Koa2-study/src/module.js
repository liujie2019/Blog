// const fs = require('fs');
// import fs from 'fs';
import {readFile} from 'fs';
// import export

// 获取fs模块中的方法
// 以下两种都是运行时加载，需要先加载fs模块的所有方法
// 方式1 fs.writeFile
// 方式2 const {writeFile} = require('fs');

// ES6模块化 静态加载即在编译的时候就进行加载

// export